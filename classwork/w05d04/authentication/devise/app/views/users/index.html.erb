<div class="col-sm-12">
	<div class="user">
		<% if user_signed_in? %>
			<h1><%=current_user.name%></h1>


			Logged in as <%= current_user.email %>


		<% else %>
			<h1>Sign in Or Sign Up</h1>
			<p>No User logged in</p>

		<% end %>	
	</div>
</div>


		